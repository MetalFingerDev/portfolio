# gravity — Python environment

This folder uses a virtual environment configured at `C:/projects/protfolio/.venv`.

## Activate (Windows PowerShell)

```powershell
& C:/projects/protfolio/.venv/Scripts/Activate.ps1
```

## Activate (Windows cmd.exe)

```
C:\projects\protfolio\.venv\Scripts\activate.bat
```

## Install requirements

```
pip install -r requirements.txt
```

> Note: the `math` module is part of the Python standard library and does not need to be installed.
